var colors = ["red", "orange", "yellow", "green"];

// for (var i = 0; i < colors.length; i++) {
//      console.log(colors[i]);
// }

//arr.forEach(someFunction)

colors.forEach(function(eachColor) {
	console.log("INSIDE THE FOREACH " + eachColor);
});
